<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cdi.com.onsport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <uses-permission android:name="android.permission.INTERNET" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity android:name=".MainActivity" />
        <activity android:name=".userAccount.SignIn" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".userAccount.MonCompte" />
        <activity android:name=".ActivityController.ListeActivity" />
        <activity android:name=".ActivityController.ActivityDetails" />
        <activity android:name=".ActivityController.ProposeActivity" />
        <activity android:name=".userAccount.UserModifier" />
        <activity android:name=".ActivityController.SearchActivity" />
        <activity android:name=".ActivityController.MyActivity"></activity>
    </application>

</manifest>